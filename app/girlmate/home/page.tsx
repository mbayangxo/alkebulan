import { GMHomePage } from "@/app/components/girlmate/gm-home";

export const metadata = { title: "Home — GirlMate", description: "Browse and post women-only housing listings." };

export default function HomeRoute() {
  return <GMHomePage />;
}
