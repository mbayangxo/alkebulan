import { GMPostPage } from "@/app/components/girlmate/gm-post";

export const metadata = { title: "Post a Listing — GirlMate", description: "Add your room or roommate listing to GirlMate." };

export default function PostRoute() {
  return <GMPostPage />;
}
